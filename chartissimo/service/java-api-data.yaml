apiVersion: v1
kind: Service
metadata:
  name: user
spec:
  selector:
    app: chartissimo
  type: LoadBalancer
  ports:
    - protocol: TCP
      name: user
      port: 5000
      targetPort: 5000
      nodePort: 30008