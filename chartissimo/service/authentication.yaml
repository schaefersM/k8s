apiVersion: v1
kind: Service
metadata:
  name: auth
spec:
  selector:
    app: chartissimo
  type: LoadBalancer
  ports:
    - protocol: TCP
      name: auth
      port: 4000
      targetPort: 4000
      nodePort: 30002